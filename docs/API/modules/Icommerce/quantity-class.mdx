import Api from '@site/src/components/rest/Api';
import { TableSchema } from '@site/src/components/table/table.jsx';
import { quantityClass } from './scheme/schemeIcommerce.jsx'
import InfoAboutRequest from '../partials/_infoAboutRequest.mdx';
import Cache from '../partials/_infoAboutCache.mdx';

# QuantityClass
Use the REST API to interact with the quantityClass in the application.

<InfoAboutRequest />
<Cache />

## Create a quantityClass
Use this endpoint to create a quantityClass. You can only have one default as `true`.

<Api
  method="POST"
  url="/api/icommerce/v3/quantity-classes"
  minimum={`{
  "attributes": {
    "value": 6,
    "en": { //This depends on the server's language
      "title": "Sixpack",
      "unit": "sixpack",
    }
  }
}`}
  request={`{
  "attributes": {
    "value": 6,
    "default": true,
    "en": {
      "title": "Sixpack",
      "unit": "sixpack",
    }
  }
}`}
  responseSample={`{
  "data": {
    "id": 1,
    "title": "Sixpack",
    "default": true,
    "unit": "sixpack",
    "value": 6
  }
}`}
/>

---
## Modify a quantityClass

Use this endpoint to modify a quantityClass. You can send one or multiple values in the request.

By default, the accepted value is the ID. However, if you want to send another value, you must include it in the `Query Parameters`:

```json
{
  "filter": {
    "field": "title" // Value by which the search will be performed
  }
}
```

<Api
  method="PUT"
  url="/api/icommerce/v3/quantity-classes/{criteria}"
  minimum={`{
  "attributes": {
    "en": {
      "title": "Unit",
    }
  }
}`}
  request={`{
  "attributes": {
    "value": 1,
    "default": true,
    "en": {
      "title": "Unit",
      "unit": "und",
    }
  }
}`}
  responseSample={`{
  "data": "Item Updated"
}`}
/>

---
## Get Multiple quantityClass

Use this endpoint to retrieve multiple quantityClass.

You can filter quantityClass and paginate the results.

`Query Parameters`:

<Api
  method="GET"
  url="/api/icommerce/v3/quantity-classes"
  request={`{
  "filter": {
    "order": {
      "field":"created_at",
      "way":"desc"
    }
  },
  "page": 1,
  "take": 1
}`}
  responseSample={`{
  "data": [
    {
      "id": 1,
      "title": "Sixpack",
      "default": true,
      "unit": "sixpack",
      "value": 6
    }
  ],
  "meta": {
    "page": {
      "total": 1,
      "lastPage": 1,
      "perPage": "1",
      "currentPage": 1
    }
  }
}`}
/>

---
## Get a quantityClass

Use this endpoint to retrieve a specific quantityClass.

<Api
  method="GET"
  url="/api/icommerce/v3/quantity-classes/{criteria}"
  request={`{}`}
  responseSample={`{
  "data": {
    "id": 1,
    "title": "Sixpack",
    "default": true,
    "unit": "sixpack",
    "value": 6
  }
}`}
/>

---
## Delete a quantityClass

Use this endpoint to delete a specific quantityClass.

Send the quantityClass's ID to delete it.

<Api
  method="DELETE"
  url="/api/icommerce/v3/quantity-classes/{id}"
  request={`{}`}
  responseSample={`{
  "data": "Request successful"
}`}
/>

---
## QuantityClass Fields

<TableSchema columnNames={["Name", "Description"]} data={quantityClass} />

